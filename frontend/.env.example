# Backend API URL (proxied through Vite in dev; set for production)
VITE_API_URL=http://localhost:3001

# WebSocket URL (auto-detected from window.location in dev)
# VITE_WS_URL=ws://localhost:3001/ws
